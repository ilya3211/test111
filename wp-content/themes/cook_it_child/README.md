# Cook It Child Theme - Дизайн "К СЕБЕ"
## Полная документация и карта стилей

**Версия:** 1.0
**Дата:** Ноябрь 2024
**Тема:** Cook It Child
**Дизайн-система:** К СЕБЕ

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Структура файлов](#структура-файлов)
3. [Карта стилей](#карта-стилей)
4. [Компоненты](#компоненты)
5. [CSS переменные](#css-переменные)
6. [Быстрый старт](#быстрый-старт)
7. [Кастомизация](#кастомизация)
8. [Решение проблем](#решение-проблем)

---

## 🎯 Обзор

Детская тема Cook It с полностью переработанным дизайном карточек рецептов в стиле современного сервиса "К СЕБЕ".

### Что изменено:
✅ Карточки рецептов (стиль, цвета, типографика)
✅ Система рейтингов (дизайн звёзд)
✅ Блоки пищевой ценности
✅ Badges категорий (цвета, позиционирование)
✅ Мета-информация (иконки, стили)
✅ Адаптивность (mobile-first)

### Что НЕ изменено:
❌ Основная структура темы
❌ Header/Footer
❌ Sidebar
❌ Функционал WordPress

---

## 📁 Структура файлов

```
wp-content/themes/cook_it_child/
│
├── style.css                          # Основной файл темы
├── style-k-sebe-cards.css            # ⭐ К СЕБЕ стили для карточек
├── functions.php                      # Подключение стилей + кастомизация
│
├── template-parts/
│   ├── hero/
│   │   └── hero-section.php          # Hero блок для главной
│   └── posts/
│       └── content-card-line.php     # Шаблон карточки (дочерняя)
│
├── js/
│   └── header-menu.js                # Скрипты меню
│
└── README.md                          # Основная документация
```

---

## 🗺️ Карта стилей

### style-k-sebe-cards.css (18 KB)

**Главный файл стилей К СЕБЕ.** Содержит все переработанные компоненты.

#### Структура файла:

```
1. CSS ПЕРЕМЕННЫЕ (строки 1-20)
   ├── Цвета (primary, text, backgrounds)
   ├── Тени (shadows)
   └── Радиусы (border-radius)

2. ОБЩИЕ СТИЛИ КАРТОЧКИ (21-80)
   ├── .content-card
   ├── .content-card--line
   └── .content-card__body

3. ИЗОБРАЖЕНИЯ КАРТОЧКИ (81-150)
   ├── .content-card__image
   ├── Позиционирование
   └── Адаптивность изображений

4. ЗАГОЛОВКИ (151-180)
   ├── .content-card__title
   ├── Типографика
   └── Hover эффекты

5. МЕТА ИНФОРМАЦИЯ (181-240)
   ├── .content-card__meta
   ├── .meta-cooking-time, .meta-serves
   ├── Иконки (💸, 👥, 👁, 💬)
   └── Адаптивность

6. EXCERPT/ОПИСАНИЕ (241-270)
   └── .content-card__excerpt

7. ПИЩЕВАЯ ЦЕННОСТЬ (271-310)
   ├── .nutritional
   ├── .nutritional-list
   ├── Предотвращение overlap текста
   └── Gap и flex стили

8. ССЫЛКА КАРТОЧКИ (311-340)
   └── .content-card__link

9. CATEGORY BADGE (341-410)
   ├── .entry-category
   ├── Позиционирование (bottom-left)
   ├── Цвета (оранжевый #FF9966)
   ├── Размеры (ultra-compact)
   ├── Все состояния ссылок
   └── Фиксация при hover

10. РЕЙТИНГ (411-520)
    ├── .entry-rating
    ├── .wp-star-rating
    ├── Градиент фона
    ├── Золотые звёзды (#FFB800)
    ├── Правильное отображение дробных оценок
    └── Тени для звёзд

11. АДАПТИВНОСТЬ (521-700+)
    ├── @media (max-width: 968px) - Планшеты
    ├── @media (max-width: 640px) - Мобильные
    └── Responsive изображения, типографика, spacing
```

---

## 🎨 Компоненты

### 1. Карточка рецепта (.content-card)

**Местоположение:** `style-k-sebe-cards.css` (строки 21-80)

**HTML структура:**
```html
<div class="content-card content-card--line">
    <div class="content-card__image">
        <span class="entry-category">Категория</span>
        <img src="..." alt="...">
    </div>
    <div class="content-card__body">
        <h3 class="content-card__title">Заголовок</h3>
        <div class="content-card__meta">...</div>
        <div class="content-card__excerpt">...</div>
        <div class="nutritional">...</div>
        <div class="entry-rating">...</div>
    </div>
</div>
```

**Ключевые стили:**
- Фон: `#fff`
- Тень: `0 2px 8px rgba(0,0,0,0.05)`
- Border-radius: `12px`
- Padding: `20px`
- Transition: `all 0.3s ease`

**Hover эффект:**
- Transform: `translateY(-2px)`
- Box-shadow: `0 4px 15px rgba(0,0,0,0.1)`

---

### 2. Badge категории (.entry-category)

**Местоположение:** `style-k-sebe-cards.css` (строки 341-410)

**Позиция:** Левый нижний угол изображения

**Стили:**
```css
position: absolute;
bottom: 10px;
left: 10px;
background: rgba(255, 153, 102, 0.95) !important;
color: #fff;
font-size: 9px;
padding: 2px 6px;
border-radius: 8px;
```

**Важные фиксы:**
- ✅ Все состояния ссылок (`:link`, `:visited`, `:hover`)
- ✅ Прозрачный фон для `<a>` внутри badge
- ✅ Фиксация позиции при hover (не прыгает)
- ✅ Оранжевый цвет вместо синего (#4547c1)

---

### 3. Блок рейтинга (.entry-rating)

**Местоположение:** `style-k-sebe-cards.css` (строки 411-520)

**Дизайн:**
- Градиентный фон: `linear-gradient(135deg, #FFF9F0 0%, #FFFBF7 100%)`
- Border: `1px solid #FFE5CC`
- Border-radius: `12px`
- Padding: `18px 20px`
- Тень: `0 2px 8px rgba(255, 153, 102, 0.08)`

**Звёзды:**
- Цвет: `#FFB800` (золотой)
- Размер: `20px`
- Эффект: `drop-shadow(0 2px 4px rgba(255, 184, 0, 0.3))`

**Фикс дробных оценок:**
```css
/* Для рейтинга 4.65 показывает 4 звезды, а не 5 */
.star-rating--score-5 .star-rating-item svg path {
    fill: #E0E0E0; /* Все серые по умолчанию */
}
/* Затем заполняем только 1-4 звезды */
```

---

### 4. Пищевая ценность (.nutritional)

**Местоположение:** `style-k-sebe-cards.css` (строки 271-310)

**Структура:**
```html
<div class="nutritional">
    <div class="nutritional__header">На порцию</div>
    <div class="nutritional-list">
        <span>Calories2</span>
        <span><span class="strong">120</span></span>
    </div>
</div>
```

**Важные фиксы:**
- ✅ Gap между label и value (10px)
- ✅ Ellipsis для длинных названий
- ✅ Всегда видимые значения (flex: 0 0 auto)
- ✅ Предотвращение overlap текста

**Стили:**
```css
.nutritional-list {
    display: flex;
    justify-content: space-between;
    gap: 10px; /* Важно! */
}

.nutritional-list > span:first-child {
    flex: 0 1 auto;
    overflow: hidden;
    text-overflow: ellipsis;
}

.nutritional-list > span:last-child {
    flex: 0 0 auto; /* Не сжимается */
    white-space: nowrap;
}
```

---

### 5. Мета информация (.content-card__meta)

**Местоположение:** `style-k-sebe-cards.css` (строки 181-240)

**Иконки:**
- 💸 Цена/Время (`.meta-cooking-time::before`)
- 👥 Порции (`.meta-serves::before`)
- 👁 Просмотры (`.meta-views::before`)
- 💬 Комментарии (`.meta-comments::before`)
- 📅 Дата (`.meta-date::before`)

**Стили:**
```css
.meta-cooking-time::before {
    content: "💸" !important;
    font-size: 14px;
}
```

---

## 🎨 CSS Переменные

**Местоположение:** `style-k-sebe-cards.css` (строки 1-20)

```css
:root {
    /* Цвета */
    --ksebe-primary: #FF9966;           /* Основной оранжевый */
    --ksebe-primary-dark: #FF8C5A;      /* Темный оранжевый */
    --ksebe-text: #333;                 /* Основной текст */
    --ksebe-text-light: #666;           /* Светлый текст */
    --ksebe-text-lighter: #999;         /* Еще светлее */

    /* Фоны */
    --ksebe-bg: #F9F9F9;                /* Фон страницы */
    --ksebe-card-bg: #fff;              /* Фон карточки */

    /* Границы и тени */
    --ksebe-border: #e0e0e0;            /* Цвет границ */
    --ksebe-shadow-sm: 0 2px 8px rgba(0,0,0,0.05);
    --ksebe-shadow-md: 0 2px 12px rgba(0,0,0,0.06);

    /* Радиусы */
    --ksebe-radius: 12px;               /* Основной радиус */
    --ksebe-radius-sm: 8px;             /* Маленький радиус */
}
```

**Использование:**
```css
.my-element {
    background-color: var(--ksebe-card-bg);
    color: var(--ksebe-primary);
    border-radius: var(--ksebe-radius);
}
```

---

## 🚀 Быстрый старт

### 1. Применить изменения

В Gitium (WordPress Admin):
1. Зайти в **Gitium** → **Commit**
2. Нажать **Pull** или **Merge**
3. Очистить кеш WordPress

### 2. Проверить результат

Откройте любую страницу категории:
- `http://yoursite.com/category/recipes/`
- `http://yoursite.com/tags/ingredient/`

### 3. Автоматическое обновление

CSS использует `filemtime()` для версионирования:
- При изменении файла версия обновляется автоматически
- Не нужно вручную чистить кеш

---

## 🎨 Кастомизация

### Изменить основной цвет

**В файле:** `style-k-sebe-cards.css` (строка 2)

```css
:root {
    --ksebe-primary: #3498db; /* Синий вместо оранжевого */
}
```

Это изменит:
- Badge категорий
- Иконки
- Активные элементы

### Изменить размер badge

**В файле:** `style-k-sebe-cards.css` (строка ~390)

```css
.content-card__image .entry-category {
    font-size: 11px !important;    /* Было 9px */
    padding: 4px 10px !important;  /* Было 2px 6px */
}
```

### Изменить цвет звёзд

**В файле:** `style-k-sebe-cards.css` (строка ~470)

```css
.star-rating-item svg path {
    fill: #FF6B6B !important; /* Красные звёзды */
}
```

### Отключить тени

**В файле:** `style-k-sebe-cards.css`

```css
.content-card {
    box-shadow: none !important;
}

.entry-rating {
    box-shadow: none !important;
}
```

### Изменить радиус скругления

**В файле:** `style-k-sebe-cards.css` (строка 13)

```css
:root {
    --ksebe-radius: 8px;  /* Было 12px (более острые углы) */
}
```

---

## 📱 Адаптивность

### Breakpoints

```css
/* Планшеты */
@media (max-width: 968px) {
    /* Grid карточек: 2 колонки */
}

/* Мобильные */
@media (max-width: 640px) {
    /* Grid карточек: 1 колонка */
    /* Уменьшенные отступы */
    /* Компактные размеры */
}
```

### Что изменяется

| Элемент | Desktop | Tablet | Mobile |
|---------|---------|--------|--------|
| Badge font-size | 9px | 9px | 8px |
| Title font-size | 20px | 18px | 16px |
| Card padding | 20px | 18px | 15px |
| Image height | auto | auto | 200px |

---

## 🐛 Решение проблем

### Стили не применяются

**Причина:** Кеширование

**Решение:**
1. Очистить кеш WordPress (WP Super Cache, W3 Total Cache)
2. Очистить кеш браузера (Ctrl+Shift+R)
3. Проверить что файл подключен: View Source → найти `style-k-sebe-cards.css`

### Синий фон на badge

**Причина:** Inline стиль от WordPress Customizer

**Решение:** Уже исправлено в `style-k-sebe-cards.css`
```css
.entry-category a {
    background-color: transparent !important;
}
```

### Текст в nutritional накладывается

**Причина:** Длинные названия

**Решение:** Уже исправлено с помощью flex и gap
```css
.nutritional-list {
    gap: 10px;
}
```

### Рейтинг 4.65 показывает 5 звёзд

**Причина:** WordPress округляет до 5

**Решение:** Уже исправлено с помощью специфичных селекторов
```css
.star-rating--score-5 .star-rating-item:nth-child(5) svg path {
    fill: #E0E0E0 !important; /* 5-я звезда серая */
}
```

### Badge прыгает при hover

**Причина:** Transform от родительской темы

**Решение:** Уже исправлено
```css
.content-card:hover .entry-category {
    bottom: 10px !important; /* Фиксируем позицию */
}
```

---

## 📊 Изменения в functions.php

**Местоположение:** `wp-content/themes/cook_it_child/functions.php`

### 1. Подключение К СЕБЕ стилей

```php
// Строки 18-28
$css_file = get_stylesheet_directory() . '/style-k-sebe-cards.css';
$css_version = file_exists($css_file) ? filemtime($css_file) : '1.0.2';

wp_enqueue_style(
    'k-sebe-cards',
    get_stylesheet_directory_uri() . '/style-k-sebe-cards.css',
    array( 'cook-it-style-child' ),
    $css_version  // Автоматическая версия из времени модификации
);
```

**Что это делает:**
- ✅ Подключает CSS на всех страницах
- ✅ Автоматическое обновление версии при изменении файла
- ✅ Правильный порядок загрузки (после родительской темы)

### 2. Замена телефона (пример кастомизации)

```php
// Строки 47-60
function cook_it_child_replace_phone_callback($html) {
    $html = str_replace('34343', '555-0001', $html);
    return $html;
}
```

### 3. Hero секция на главной

```php
// Строки 65-71
add_action( 'cook_it_before_site_content', 'cook_it_child_add_hero_section' );
function cook_it_child_add_hero_section() {
    if ( is_front_page() || is_home() ) {
        get_template_part( 'template-parts/hero/hero-section' );
    }
}
```

### 4. Изменение slug таксономии ingredients

```php
// Строки 84-141
// /ingredients/ → /tags/
```

---

## 📝 История изменений

### v1.0.0 (Ноябрь 2024)

**Коммиты:**
- `d88165c` - Fix: Block blue inline background (#4547c1) on .entry-category links
- `c4b30b9` - Fix: Prevent nutritional info text from overlapping
- `08d17c8` - Fix: Force orange background for .entry-category
- `f9cd945` - Fix: Change cooking time icon from ⏱️ to 💸
- `67b58c5` - Fix: Use file modification time as CSS version for auto cache busting
- `3df4858` - Fix: Show 4 stars for fractional ratings (4.1-4.9)
- `d63d5da` - Redesign rating block in К СЕБЕ style
- `1aa39b1` - Fix blue link colors in all states

**Что добавлено:**
- ✅ Полная переработка стилей карточек
- ✅ Система рейтингов с золотыми звёздами
- ✅ Блоки пищевой ценности
- ✅ Ultra-compact badges
- ✅ Автоматический cache busting
- ✅ Mobile-first адаптивность
- ✅ CSS переменные для кастомизации

**Исправлено:**
- ✅ Синий фон на badge (#4547c1)
- ✅ Наложение текста в nutritional
- ✅ Неправильное отображение дробных рейтингов
- ✅ Прыгающий badge при hover
- ✅ Синие/фиолетовые ссылки в badge

---

## 🎓 Примеры кода

### Пример 1: Добавить новый badge

```css
.content-card__custom-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    background: var(--ksebe-primary);
    color: #fff;
    padding: 4px 8px;
    border-radius: var(--ksebe-radius-sm);
    font-size: 10px;
    font-weight: 600;
}
```

### Пример 2: Изменить hover эффект карточки

```css
.content-card:hover {
    transform: scale(1.02); /* Вместо translateY */
    box-shadow: 0 8px 30px rgba(0,0,0,0.15);
}
```

### Пример 3: Добавить анимацию появления

```css
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.content-card {
    animation: fadeInUp 0.5s ease-out;
}
```

---

## 🔗 Связанные файлы

### Основные
- `style-k-sebe-cards.css` - Все К СЕБЕ стили ⭐
- `functions.php` - Подключение и кастомизация
- `style.css` - Базовые стили темы

### Шаблоны
- `template-parts/posts/content-card-line.php` - Шаблон карточки
- `template-parts/hero/hero-section.php` - Hero блок

### Документация
- `README.md` - Основная документация (этот файл)

---

## 📞 Поддержка

Для вопросов и предложений:
- Создайте issue в репозитории проекта
- Проверьте раздел [Решение проблем](#решение-проблем)

---

## ⚖️ Лицензия

Этот код является частью детской темы Cook It.
Cook It Theme © WPShop.ru

---

**Последнее обновление:** Ноябрь 2024
**Версия документации:** 1.0
