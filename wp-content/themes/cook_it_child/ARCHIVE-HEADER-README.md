# Archive Header with Stats - Заголовок со статистикой

Стилизует заголовок страницы категории с блоком статистики в стиле "К СЕБЕ".

## 📦 Что включено

1. **style-archive-header.css** - CSS стили для .page-header со статистикой
2. **template-parts/archive-stats-header.php** - Готовый PHP шаблон (опционально)

## 🚀 Быстрый старт

### Вариант 1: Только CSS (автоматически)

Стили **автоматически применяются** на страницах архивов/категорий для существующего `.page-header`.

Просто добавьте HTML в шаблон:

```html
<div class="page-header">
    <h1 class="page-title">Категория рецептов</h1>
    <div class="stats">
        <div class="stat-item">
            <span class="stat-icon">🔍</span>
            <span><strong>Найдено:</strong> 150 рецептов</span>
        </div>
        <div class="stat-item">
            <span class="stat-icon">⭐</span>
            <span><strong>Средний рейтинг:</strong> <span class="rating-stars">4.8 ★★★★★</span></span>
        </div>
    </div>
</div>
```

### Вариант 2: Использовать готовый PHP шаблон

В `archive.php`, `category.php` или `taxonomy.php` добавьте **перед циклом постов**:

```php
<?php get_template_part( 'template-parts/archive-stats-header' ); ?>
```

### Вариант 3: Через хук WordPress

В `functions.php`:

```php
add_action('cook_it_before_posts_loop', function() {
    get_template_part( 'template-parts/archive-stats-header' );
}, 10);
```

## 🎨 Внешний вид

```
┌─────────────────────────────────────────┐
│  Категория: Морепродукты                │
│                                         │
│  🔍 Найдено: 150 рецептов              │
│  ⭐ Средний рейтинг: 4.8 ★★★★★         │
└─────────────────────────────────────────┘
```

## 🎯 Классы и структура

```html
<div class="page-header">
    <h1 class="page-title">Заголовок</h1>
    <div class="stats">
        <div class="stat-item">
            <span class="stat-icon">🔍</span>
            <span>Текст статистики</span>
        </div>
    </div>
</div>
```

### Основные классы:

- `.page-header` - белая карточка с тенью
- `.page-title` - крупный заголовок (32px)
- `.stats` - контейнер статистики
- `.stat-item` - отдельный элемент статистики
- `.stat-icon` - иконка (оранжевая)
- `.rating-stars` - жёлтые звёздочки

## 🎨 Варианты отображения

### Вертикальная статистика (по умолчанию)
```html
<div class="page-header">
    ...
</div>
```

### Горизонтальная статистика (на широких экранах)
```html
<div class="page-header stats-horizontal">
    ...
</div>
```

### Статистика в карточках
```html
<div class="page-header stats-cards">
    ...
</div>
```

Карточки будут с hover эффектом и левой оранжевой границей.

## 📱 Адаптивность

| Устройство | Заголовок | Статистика |
|------------|-----------|------------|
| Desktop (>1200px) | 32px | Вертикально |
| Tablet (640-968px) | 28px | Вертикально |
| Mobile (<640px) | 24px | Вертикально |

## 🎨 Кастомизация CSS

Основные переменные находятся в файле, но можно переопределить:

```css
/* В вашем style.css */
.page-header {
    background-color: #fff; /* Фон карточки */
    border-radius: 12px;    /* Скругление */
}

.page-header .stats .stat-icon {
    color: #FF9966; /* Цвет иконок */
}

.page-header .stats .rating-stars {
    color: #FFB800; /* Цвет звёзд */
}
```

## 📊 Подключение данных из WordPress

Готовый PHP шаблон автоматически получает:
- ✅ Количество постов в категории
- ⚠️ Средняя цена (нужно настроить)
- ⚠️ Средний рейтинг (нужно настроить)

Чтобы добавить реальные данные, отредактируйте `archive-stats-header.php`:

```php
// Пример: подсчет средней цены из custom fields
function calculate_average_price( $term ) {
    $posts = get_posts([
        'post_type' => 'post',
        'posts_per_page' => -1,
        'tax_query' => [[
            'taxonomy' => 'category',
            'field' => 'term_id',
            'terms' => $term->term_id
        ]]
    ]);

    $total_price = 0;
    $count = 0;

    foreach ( $posts as $post ) {
        $price = get_post_meta( $post->ID, 'recipe_price', true );
        if ( $price ) {
            $total_price += floatval( $price );
            $count++;
        }
    }

    return $count > 0 ? $total_price / $count : 0;
}
```

## 🔧 Где использовать

CSS применяется автоматически на:
- ✅ Страницах категорий (`is_category()`)
- ✅ Страницах таксономий (`is_tax()`)
- ✅ Страницах архивов (`is_archive()`)
- ✅ Главной странице (`is_home()`)

## 🐛 Решение проблем

### Стили не применяются
1. Очистите кеш WordPress
2. Проверьте что файл `style-archive-header.css` существует
3. Убедитесь что вы на странице архива/категории

### Статистика не показывается
1. Проверьте что добавили HTML блок `.page-header`
2. Или используйте готовый PHP шаблон
3. Проверьте что в категории есть посты

### Неправильные данные
Отредактируйте `archive-stats-header.php` и добавьте свою логику подсчёта статистики.

## 📝 Примеры использования

### Простой заголовок без статистики
```html
<div class="page-header">
    <h1 class="page-title">Рецепты из морепродуктов</h1>
</div>
```

### С одним элементом статистики
```html
<div class="page-header">
    <h1 class="page-title">Рецепты из морепродуктов</h1>
    <div class="stats">
        <div class="stat-item">
            <span class="stat-icon">🔍</span>
            <span><strong>Найдено:</strong> 150 рецептов</span>
        </div>
    </div>
</div>
```

### Полная статистика
```html
<div class="page-header">
    <h1 class="page-title">Рецепты из морепродуктов</h1>
    <div class="stats">
        <div class="stat-item">
            <span class="stat-icon">🔍</span>
            <span><strong>Найдено:</strong> 150 рецептов</span>
        </div>
        <div class="stat-item">
            <span class="stat-icon">💰</span>
            <span><strong>Цена:</strong> мин. 200 руб. средн. 500 руб. макс. 1500 руб.</span>
        </div>
        <div class="stat-item">
            <span class="stat-icon">⭐</span>
            <span><strong>Средний рейтинг:</strong> <span class="rating-stars">4.8 ★★★★★</span></span>
        </div>
    </div>
</div>
```

## 📞 Поддержка

Файлы созданы для темы Cook It (К СЕБЕ дизайн).
