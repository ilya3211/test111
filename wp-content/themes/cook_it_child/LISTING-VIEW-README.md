# Listing View - Стиль "К СЕБЕ" для страниц категорий

Трансформация стандартных карточек рецептов в большой горизонтальный листинг в стиле современных сервисов.

## 📦 Что включено

1. **style-listing-view.css** - Основные стили листинга
2. **template-parts/listing-controls.php** - Блок управления (сортировка, счетчик)
3. **template-parts/category-info-banner.php** - Информационный баннер

## 🚀 Быстрый старт

### Автоматическая активация

Стили **автоматически применяются** на всех страницах:
- Категорий (category)
- Архивов (archive)
- Таксономий (taxonomy)
- Главной (home)

Просто очистите кеш WordPress и обновите страницу!

### Ручная активация (опционально)

Если хотите больше контроля, можете отключить автозагрузку в `functions.php`:

```php
// Закомментируйте строки 32-42 в functions.php
/*
if ( is_archive() || is_category() || is_tax() || is_home() ) {
    wp_enqueue_style(
        'k-sebe-listing-view',
        get_stylesheet_directory_uri() . '/style-listing-view.css',
        array( 'k-sebe-cards' ),
        $listing_css_version
    );
}
*/
```

Затем подключите на конкретных страницах:

```php
// В archive.php или category.php
wp_enqueue_style( 'k-sebe-listing-view', get_stylesheet_directory_uri() . '/style-listing-view.css' );
```

## 🎨 Что изменяется

### До (стандартные карточки):
```
┌─────┐ ┌─────┐ ┌─────┐
│ IMG │ │ IMG │ │ IMG │
├─────┤ ├─────┤ ├─────┤
│Title│ │Title│ │Title│
└─────┘ └─────┘ └─────┘
```

### После (листинг):
```
┌──────────────────────────────┐
│ ┌────┐                       │
│ │IMG │ Заголовок рецепта      │
│ │    │ ★★★★★ 5.0 • 10 отзывов│
│ └────┘ Описание...            │
│        [Подробнее] [Готовить] │
└──────────────────────────────┘
```

## 📋 Дополнительные блоки (опционально)

### 1. Блок управления листингом

Добавьте в начало `archive.php` или `category.php`:

```php
<?php get_template_part( 'template-parts/listing-controls' ); ?>
```

**Что дает:**
- Счетчик найденных рецептов
- Сортировка по: дате, названию, популярности, рейтингу
- Автоматическое сохранение параметров URL

### 2. Информационный баннер

Добавьте после заголовка категории:

```php
<?php get_template_part( 'template-parts/category-info-banner' ); ?>
```

**Что дает:**
- Зеленый баннер с информацией о проверенных рецептах
- Можно кастомизировать текст

## 🎯 Настройка цветов

Все цвета вынесены в CSS переменные (начало файла `style-listing-view.css`):

```css
:root {
    --listing-primary: #FF9966;        /* Основной оранжевый */
    --listing-primary-dark: #FF8C5A;   /* Темнее оранжевый */
    --listing-text: #333;              /* Основной текст */
    --listing-text-light: #666;        /* Светлый текст */
    --listing-bg: #F9F9F9;             /* Фон страницы */
    --listing-card-bg: #fff;           /* Фон карточки */
    --listing-shadow-sm: 0 2px 12px rgba(0,0,0,0.06);
    --listing-radius: 16px;            /* Радиус скругления */
}
```

**Пример изменения цвета:**

```css
:root {
    --listing-primary: #3498db;        /* Синий вместо оранжевого */
}
```

## 📱 Адаптивность

Стили полностью адаптивны:

- **Desktop (>968px)**: Горизонтальные карточки с изображением слева
- **Tablet (640-968px)**: Вертикальные карточки с изображением сверху
- **Mobile (<640px)**: Компактные карточки с уменьшенными отступами

## ⚡ Производительность

### Автоматический cache busting
Версия CSS файла обновляется автоматически при изменении файла (используется `filemtime()`).

Не нужно вручную чистить кеш после изменений!

### Анимации
Плавное появление карточек с `fadeInUp` анимацией:

```css
.content-card.content-card--line {
    animation: fadeInUp 0.5s ease-out;
}
```

Чтобы отключить анимацию, закомментируйте в конце файла:

```css
/* .content-card.content-card--line {
    animation: fadeInUp 0.5s ease-out;
} */
```

## 🔧 Кастомизация

### Изменить размер изображения карточки

```css
.content-card.content-card--line {
    grid-template-columns: 320px 1fr; /* Было 280px */
}

.content-card--line .content-card__image {
    width: 320px !important;
}
```

### Изменить отступы между карточками

```css
:root {
    --listing-spacing: 40px; /* Было 30px */
}
```

### Изменить радиус скругления

```css
:root {
    --listing-radius: 8px; /* Было 16px (более острые углы) */
}
```

### Добавить кнопки к карточкам

В конец `content-card-line.php` перед `</div>` добавьте:

```php
<div class="card-actions">
    <a href="<?php the_permalink(); ?>" class="btn btn-primary">
        Смотреть рецепт
    </a>
</div>
```

## 🐛 Решение проблем

### Стили не применяются
1. Очистите кеш WordPress (плагины кеширования)
2. Очистите кеш браузера (Ctrl+Shift+R)
3. Проверьте что файл `style-listing-view.css` существует
4. Проверьте консоль браузера на ошибки 404

### Карточки не горизонтальные
Проверьте что у вас используется шаблон `content-card-line.php` (не `content-card-big.php`).

### Конфликт с другими стилями
Стили используют `!important` для переопределения. Если всё равно не работает:

1. Увеличьте специфичность:
```css
body.archive .content-card.content-card--line {
    /* ваши стили */
}
```

2. Загружайте CSS последним (измените приоритет в `functions.php`):
```php
add_action( 'wp_enqueue_scripts', 'enqueue_child_theme_styles', 999); // Было 100
```

## 📊 Совместимость

- ✅ WordPress 5.0+
- ✅ Тема Cook It
- ✅ PHP 7.0+
- ✅ Все современные браузеры
- ✅ Мобильные устройства

## 🎓 Примеры использования

### Только для категории "Морепродукты"

В `functions.php`:

```php
add_filter( 'body_class', 'add_listing_view_class' );
function add_listing_view_class( $classes ) {
    if ( is_category( 'moreprodukty' ) ) {
        $classes[] = 'listing-view-active';
    }
    return $classes;
}
```

В `style-listing-view.css` оберните всё в:

```css
.listing-view-active .content-card.content-card--line {
    /* стили */
}
```

### Отключить для мобильных

В конец `style-listing-view.css`:

```css
@media (max-width: 640px) {
    .content-card.content-card--line {
        grid-template-columns: 1fr !important;
        /* Вернется к стандартным стилям */
    }
}
```

## 📞 Поддержка

Файлы созданы для темы Cook It (К СЕБЕ дизайн).

Для вопросов и предложений создайте issue в репозитории проекта.

## 📝 Changelog

### v1.0.0 (2025-01-04)
- Первый релиз
- Трансформация карточек в горизонтальный листинг
- Адаптивный дизайн
- Автоматический cache busting
- Блок управления листингом
- Информационный баннер
